// This file is for API endpoints if needed in the future
// For now, it's a placeholder for server-side logic

export default {
  async fetch(request: Request) {
    // Server-side API routes can be added here
    return new Response("Not found", { status: 404 });
  },
};
      });
    }
  },
};
